<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App_3.Views.PaymentReceivedPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="20" Spacing="15">

        <TextBlock Text="Payments Received" FontSize="32" FontWeight="Bold" Margin="0,0,0,20"/>
        <!-- Top Section -->
        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBox x:Name="CustomerBox" Width="150" Header="Customer"/>
            <TextBox x:Name="AmountBox" Width="100" Header="Amount"/>
            <ComboBox x:Name="ModeBox" Width="120" Header="Mode">
                <ComboBoxItem Content="Cash"/>
                <ComboBoxItem Content="Card"/>
                <ComboBoxItem Content="Online"/>
            </ComboBox>
            <TextBox x:Name="NotesBox" Width="200" Header="Notes"/>
            <Button Content="Add Payment" Click="AddPayment_Click"/>
        </StackPanel>

        <!-- Filters Section -->
        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBox x:Name="SearchBox" Width="200" PlaceholderText="Search..." TextChanged="SearchBox_TextChanged"/>
            <DatePicker x:Name="DateFilter" DateChanged="DateFilter_DateChanged"/>
            <ComboBox x:Name="ModeFilter" Width="120" SelectionChanged="ModeFilter_SelectionChanged">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="Cash"/>
                <ComboBoxItem Content="Card"/>
                <ComboBoxItem Content="Online"/>
            </ComboBox>
        </StackPanel>

        <!-- DataGrid Section -->
        <controls:DataGrid x:Name="PaymentsGrid"
                           AutoGenerateColumns="False"
                           Height="300">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Customer" Binding="{Binding Customer}"/>
                <controls:DataGridTextColumn Header="Amount" Binding="{Binding Amount}"/>
                <controls:DataGridTextColumn Header="Mode" Binding="{Binding Mode}"/>
                <controls:DataGridTextColumn Header="Notes" Binding="{Binding Notes}"/>
                <controls:DataGridTextColumn Header="Date" Binding="{Binding DateFormatted}"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>

    </StackPanel>
</Page>



