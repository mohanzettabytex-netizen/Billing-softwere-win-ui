<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App_3.Views.InvoicePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App_3.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="20" Spacing="15">

            <TextBlock Text="Invoice / Billing" FontSize="32" FontWeight="Bold"/>

            <!-- Item Input -->
            <Grid ColumnSpacing="10" RowSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="ItemNameInput" PlaceholderText="Item Name" Grid.Row="0" Grid.Column="0"/>
                <TextBox x:Name="QuantityInput" PlaceholderText="Quantity" Grid.Row="0" Grid.Column="1"/>
                <TextBox x:Name="PriceInput" PlaceholderText="Price" Grid.Row="0" Grid.Column="2"/>
                <Button Content="Add Item" Click="AddItem_Click" Grid.Row="0" Grid.Column="3" Width="100"/>
            </Grid>

            <!-- Item List -->
            <ListView x:Name="ItemList" Height="400">
                <ListView.Header>
                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Item" FontWeight="Bold" Grid.Column="0"/>
                        <TextBlock Text="Quantity" FontWeight="Bold" Grid.Column="1"/>
                        <TextBlock Text="Price" FontWeight="Bold" Grid.Column="2"/>
                        <TextBlock Text="Total" FontWeight="Bold" Grid.Column="3"/>
                    </Grid>
                </ListView.Header>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:Item">
                        <Grid ColumnSpacing="10" Padding="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{x:Bind Name}" Grid.Column="0"/>
                            <TextBlock Text="{x:Bind Quantity}" Grid.Column="1"/>
                            <TextBlock Text="{x:Bind Price}" Grid.Column="2"/>
                            <TextBlock Text="{x:Bind Total}" Grid.Column="3"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Grand Total -->
            <TextBlock x:Name="GrandTotalText" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,10,10,0"/>
        </StackPanel>
    </ScrollViewer>
</Page>


